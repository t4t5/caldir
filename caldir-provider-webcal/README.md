# Webcal provider

The webcal provider subscribes to public `.ics` feeds, such as holiday
calendars.

Webcal calendars have no account or stored session and are always read-only.
Events are never pushed back to the source feed.
