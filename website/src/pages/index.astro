---
import BaseLayout from "../layouts/BaseLayout.astro";
import TerminalDemo from "../components/TerminalDemo.astro";
---

<BaseLayout title="caldir - your calendar as a directory">
  <main class="max-w-[640px] mx-auto px-6 py-20">
    <div class="flex items-center gap-3 mb-1">
      <img class="w-9 h-9 block" src="/logo.svg" alt="caldir logo">
      <h1 class="text-[2rem] font-bold my-0">caldir</h1>
    </div>
    <p class="text-muted text-lg mb-8">"file over app" for calendars.</p>

    <div class="mb-6">
      <TerminalDemo />
    </div>

    <p class="mb-4"><strong>caldir</strong> syncs with Google Calendar and iCloud, keeping a local directory of human-readable <code class="bg-code-bg px-1.5 py-0.5 rounded text-sm">.ics</code> files. Your calendar becomes something you and your LLM can <code class="bg-code-bg px-1.5 py-0.5 rounded text-sm">ls</code>, <code class="bg-code-bg px-1.5 py-0.5 rounded text-sm">grep</code>, and reason about.</p>

    <div class="bg-terminal-bg text-terminal-text px-5 py-4 rounded-lg font-mono text-[0.95rem] max-md:text-[0.8rem] mb-4 overflow-x-auto whitespace-nowrap before:content-['$_'] before:text-terminal-muted before:select-none">curl -sSf https://caldir.org/install.sh | sh</div>

    <div class="mt-8 flex gap-6">
      <a class="text-link hover:underline" href="/docs/what-is-caldir">Docs</a>
      <a class="text-link hover:underline" href="https://github.com/t4t5/caldir">GitHub</a>
      <a class="text-link hover:underline" href="/privacy">Privacy</a>
    </div>
  </main>
</BaseLayout>
